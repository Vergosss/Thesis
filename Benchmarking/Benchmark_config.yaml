compute_environment: LOCAL_MACHINE
deepspeed_config: {} #Experiments are DDP type, no deepspeed
distributed_type: MULTI_GPU #Basically DDP
fsdp_config: {} #Experiments are DDP type, no FSDP
machine_rank: 0 
main_process_ip: null #used for clusters not needed for these experiments
main_process_port: null #used for clusters not needed for these experiments
main_training_function: main
mixed_precision: fp16
num_machines: 1 #Experiment will run on a single machine
use_cpu: false #We are using GPUs, no CPU involved